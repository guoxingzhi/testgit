trie树(字典表)：适合用于用于搜索引擎的关键字提示，用户输入一定的字符后，提示余下的字符。
	数据结构思想：将字符串集合的相同前缀字符进行合并，将字符构建成树，实现快速搜索。
	适用于trie树的条件：
		1.字符串集合的字符集不能太大，大了之后内存空间会消耗多。即使做优化，也是在牺牲性能作为条件。
		2.字符串的前缀重合比较多。
	问题：
		1.使用trie树时，需要自己实现。
		2.使用到了指针，CPU读取数据变得不友好，性能打折扣。
	时间复杂度：O(m),m为要搜索的字符长度。